// Auto-generated by build-values-types.js. Do not edit manually.

import type { JSONTemplate } from './design';
import type { AICopilotDataType, AIFeatureType, AIProvider, CreditConfiguration, CreditPurchaseReason, CreditResource, Embed } from './editor';
import type { Image } from './features';
import type { Locale } from './intl';
import type { MergeTags } from './merge-tags';

export interface ExporterConfig {
    toSafeHtml?: (text: string | undefined, options?: any) => string;
    textDirection?: string | undefined;
    generateHtmlFromTextJson?: (textJson: any, options?: {
        inlineScriptLineHeight?: boolean;
    }) => string;
    cdnBaseUrl?: string;
}

export type Device = 'desktop' | 'mobile' | 'tablet';

export type Resolution = {
    value: number;
    name: string;
};

export type DisplayMode = 'email' | 'web' | 'popup' | 'document';

export type Variant = 'amp' | null;

export type UndoRedoBehavior = 'save' | 'replace' | 'skip';

export type Collection = 'pages' | 'bodies' | 'rows' | 'headers' | 'footers' | 'columns' | 'contents';

export interface Location {
    collection: Collection;
    id: number | string;
}

export interface CreditPurchaseRequest {
  /** Unique id for this CTA attempt. Hosts can use it as an idempotency key. */
  requestId: string;
  resource: CreditResource;
  reason: CreditPurchaseReason;
  credits: CreditConfiguration;
}

export type CreditPurchaseResult =
  | { status: 'success'; credits: CreditConfiguration }
  | { status: 'cancelled' }
  | { status: 'error'; message?: string };

type AICopilotCancelReason =
  | 'stop_button'
  | 'new_request'
  | 'item_removed'
  | 'timeout';

export interface AIAssistantUsage {
    totalTokens?: number;
    inputTokens?: number;
    outputTokens?: number;
    reasoningTokens?: number;
    cachedInputTokens?: number;
    cachedInputWrite5mTokens?: number;
    cachedInputWrite1hTokens?: number;
    /** Marked-up integer AI credits consumed by the complete turn. */
    aiCreditsUsed?: number;
    /** Raw provider cost in micro-USD; available only in local/dev/QA. */
    estimatedCostMicroUsd?: number;
}
/** Embed-level context attached to every AI Assistant lifecycle event
 * (`ai:assistant:on:request` / `:success` / `:error` / `:cancel`) so hosts
 * can attribute turns to a project, workspace, and end-user without
 * joining against their own state. */

export interface AIAssistantEventContext {
    /** Numeric Unlayer project id from `unlayer.init({ projectId })`.
     * Null when the editor runs without a project id. */
    projectId: number | null;
    /** Id of the workspace the project belongs to. Null when the editor
     * session payload doesn't include it (older API deployments). */
    workspaceId: number | null;
    /** Id of the end-user identified via `unlayer.init({ user })`. Null
     * when no end-user is identified. */
    endUserId: string | number | null;
    /** Partner-facing classification of the AI feature used this turn
     * ('full_template_gen' | 'block_edit' | 'html_import' |
     * 'image_import' | 'image_generation'), derived from the turn's
     * internal data type. Null when the turn has no partner-facing
     * classification. */
    featureType: AIFeatureType | null;
}
/** Snapshot of a generation request. Same shape that drives the
 * editor's own Redux dispatch — given to host callbacks so they can
 * route, log, or take over. */

export interface AIAssistantRequestParams extends AIAssistantEventContext {
    requestId: string;
    location: Location;
    prompt: string;
    promptId: string | undefined;
    promptFromId: string | undefined;
    data: object | null;
    dataType: AICopilotDataType;
    toolType: string | undefined;
    schemaVersion: number;
    provider: AIProvider | undefined;
    model: string | undefined;
}
/** Single internal stream event delivered to `ai:assistant:on:stream`.
 * Discriminated by `type` — switch on it to handle each kind. Fires
 * only when the editor owns generation (not when the host registered
 * `ai:assistant:generate`, since the host has its own pipeline then). */

export type AIAssistantStreamEvent = {
    type: 'start';
    attempt: number;
    messageId?: string;
    provider?: string;
    modelId?: string;
    toolType?: string | null;
} | {
    type: 'status';
    text: string;
} | {
    type: 'text-delta';
    delta: string;
    blockId?: string;
} | {
    type: 'suggest-options';
    options: Array<{
        label: string;
        prompt: string;
    }>;
} | {
    type: 'design-partial';
    full: object;
} | {
    type: 'design-operation';
    op: Record<string, any>;
};
/** Result the host returns from `ai:assistant:generate` to deliver a
 * final answer to the editor. */

export interface AIAssistantGenerateResult {
    success?: boolean;
    error?: string | {
        name: string;
        message: string;
        statusCode?: number;
    };
    statusCode?: number;
    data: object[];
    provider: AIProvider | undefined;
    model: string | undefined;
    attempt?: number;
    durationMs?: number;
    locale?: string | null;
    usage?: AIAssistantUsage;
}

export type CreditPurchaseHandlerResult = CreditPurchaseResult | Promise<CreditPurchaseResult | void> | void;

export interface CallbackContext {
    item: {
        type: string;
        id: string | number;
    };
    column?: {
        id: string | number;
    };
    row?: {
        id: string | number;
    };
}

export type InboxPreviewViewSource = 'automatic' | 'gallery_click' | 'navigation';
/** A completed Inbox Preview screenshot that became visible to the user. */

export interface InboxPreviewViewedEvent {
    /** Opaque identifier shared by every screenshot from the same preview run. */
    runId: string;
    /** End-user id supplied through `unlayer.init({ user })`, or null. */
    endUserId: string | number | null;
    /** Stable email-client identifier from the Inbox Previews client catalog. */
    clientId: string;
    /** Customer-facing name of the email client or application. */
    displayName: string;
    /** Normalized category used by customer reporting. */
    category: 'web' | 'mobile' | 'desktop';
    browser?: string;
    operatingSystem?: string;
    /** How the screenshot became visible in the editor. */
    viewSource: InboxPreviewViewSource;
    /** True when the editor selected the screenshot automatically. */
    autoSelected: boolean;
}

export interface CallbackTypeMap<TDisplayMode extends DisplayMode | undefined = undefined> {
    'editor:ready': () => void;
    'textEditor:ready': () => void;
    'design:loaded': (data: {
        design: JSONTemplate<TDisplayMode>;
    }) => void;
    /** @deprecated Use design:loaded instead */
    onDesignLoad: (data: {
        design: JSONTemplate<TDisplayMode>;
    }) => void;
    'design:updated': (data: {
        type: string;
        item?: Record<string, any>;
        changes?: Record<string, any>;
    }) => void;
    'image:uploaded': (data: {
        image: {
            url: string;
            width?: number;
            height?: number;
        };
    }) => void;
    /** @deprecated Use image:uploaded instead */
    onImageUpload: (data: {
        image: {
            url: string;
            width?: number;
            height?: number;
        };
    }) => void;
    'item:selected': (data: {
        item: Record<string, any>;
        active: boolean;
    }) => void;
    'inboxPreview:viewed': (data: InboxPreviewViewedEvent) => void;
    'page:added': (data: {
        page: Record<string, any>;
    }) => void;
    'page:removed': (data: {
        page: Record<string, any>;
    }) => void;
    'body:added': (data: {
        body: Record<string, any>;
    }) => void;
    'body:removed': (data: {
        body: Record<string, any>;
    }) => void;
    'row:added': (data: {
        row: Record<string, any>;
    }) => void;
    'row:removed': (data: {
        row: Record<string, any>;
    }) => void;
    'row:moved': (data: {
        row: Record<string, any>;
    }) => void;
    'header:added': (data: {
        header: Record<string, any>;
    }) => void;
    'header:removed': (data: {
        header: Record<string, any>;
    }) => void;
    'footer:added': (data: {
        footer: Record<string, any>;
    }) => void;
    'footer:removed': (data: {
        footer: Record<string, any>;
    }) => void;
    'column:added': (data: {
        column: Record<string, any>;
    }) => void;
    'column:removed': (data: {
        column: Record<string, any>;
    }) => void;
    'content:added': (data: {
        content: Record<string, any>;
    }) => void;
    'content:removed': (data: {
        content: Record<string, any>;
    }) => void;
    'content:moved': (data: {
        content: Record<string, any>;
    }) => void;
    image: (data: {
        accepted: File[];
        attachments: File[];
    }, done: (result: {
        progress?: number;
        url?: string;
        width?: number;
        height?: number;
        id?: number | string;
        filename?: string;
        /** Abort the in-progress upload and silently reset the state back to its default ("Upload Image"), with no error shown. */
        abort?: boolean;
        /** Reset the upload state and show the "Upload Failed" state. Pass a string for a custom message. */
        error?: boolean | string;
    }) => void) => void;
    selectImage: (data: Record<string, any>, done: (result: {
        url: string;
        width?: number;
        height?: number;
        size?: number;
        id?: string | number;
    }) => void) => void;
    displayCondition: (data: Record<string, any> & {
        context: CallbackContext;
    }, done: (result: {
        type: string;
        label: string;
        description: string;
        before: string;
        after: string;
    } | null) => void) => void;
    mergeTag: (data: {
        mergeTags: MergeTags;
        mergeTagGroup?: string;
        mergeTagRule?: string;
        context: CallbackContext;
    }, done: (result: {
        name?: string;
        value: string;
    }) => void) => void;
    mergeTagRule: (data: {
        mergeTags: MergeTags;
        mergeTagGroup?: string;
        mergeTagRule?: string;
        context: CallbackContext;
    }, done: (result: {
        mergeTagGroup: string;
        mergeTagRule: string;
    }) => void) => void;
    previewHtml: (data: {
        html: string;
    }, done: (result: {
        html: string;
    }) => void) => void;
    fetchImage: (data: {
        url: string;
    }, done: (result: {
        blob: Blob;
        url?: string;
    }) => void) => void;
    'image:removed': (params: {
        id: string | number;
        projectId?: number | null;
        userId?: string | number;
    }, done: () => void) => void;
    itemAdded: (data: {
        item: Record<string, any>;
    }, done: (result: any) => void) => void;
    itemRemoved: (data: {
        item: Record<string, any>;
    }, done: (result: boolean) => void) => void;
    itemMoved: (data: {
        item: Record<string, any>;
    }, done: (result: any) => void) => void;
    'block:added': (data: Record<string, any>, done: (result: any) => void) => void;
    'block:modified': (data: Record<string, any>, done: (result: any) => void) => void;
    'block:removed': (data: Record<string, any>, done: (result: any) => void) => void;
    /** Opens the host's in-builder purchase flow. Complete by returning a result
     * (or Promise) or by calling `done`; the first completion wins. A successful
     * credit object completely replaces the current end-user snapshot. */
    'credits:purchase': (request: CreditPurchaseRequest, done: (result: CreditPurchaseResult) => void) => CreditPurchaseHandlerResult;
    /** Host owns generation. When registered, the editor delegates the AI
     * call instead of hitting its default streaming endpoint; the host
     * delivers a final result via `done`. Predates streaming — only one
     * answer per turn. For streaming hosts, prefer endpoint configuration
     * (a host-implemented SSE endpoint) once available. */
    'ai:assistant:generate': (requestParams: AIAssistantRequestParams, done: (result: AIAssistantGenerateResult) => void) => void;
    /** Companion to `ai:assistant:generate`. Fires when the editor aborts
     * the in-flight request so the host can stop its own work. */
    'ai:assistant:abort': (requestParams: AIAssistantRequestParams) => void;
    /** Fires at turn start, before any model attempt. Always fires —
     * including when the host owns generation. */
    'ai:assistant:on:request': (requestParams: AIAssistantRequestParams) => void;
    /** Fires for every internal stream event from the editor's default
     * pipeline. Does NOT fire when the host owns generation via
     * `ai:assistant:generate` — the host has its own pipeline then. */
    'ai:assistant:on:stream': (event: AIAssistantStreamEvent) => void;
    /** Turn completed successfully. Always fires. */
    'ai:assistant:on:success': (data: AIAssistantEventContext & {
        responseId?: string;
        provider?: string;
        modelId?: string;
        /** 1 = primary model; >1 = fell back to a backup model. */
        attempt?: number;
        durationMs?: number;
        /** Locale the AI detected from the user's last message. */
        locale?: string | null;
        toolType?: string;
        usage?: AIAssistantUsage;
        design: object | null;
    }) => void;
    /** Turn failed. Always fires (only fired path; cancellations get
     * `ai:assistant:on:cancel` instead). */
    'ai:assistant:on:error': (data: AIAssistantEventContext & {
        name: string;
        message: string;
        statusCode?: number;
        usage?: AIAssistantUsage;
        provider?: string;
        modelId?: string;
        attempt?: number;
        durationMs?: number;
        locale?: string | null;
    }) => void;
    /** Turn cancelled by the user. Always fires. */
    'ai:assistant:on:cancel': (data: AIAssistantEventContext & {
        reason?: AICopilotCancelReason | null;
        partialUsage?: AIAssistantUsage;
        provider?: string;
        modelId?: string;
        attempt?: number;
    }) => void;
}

export type ExporterName = "email" | "web" | "popup" | "document" | "classic" | "ampWeb" | "ampEmail";
